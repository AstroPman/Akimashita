name: scraper-therapists (staging)

# ステージングは任意のタイミングで動作確認したいため、手動実行のみ。
# 本番と同じサイトを叩くので、bot 検知を避けるため定期実行はしない。
on:
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/scraper-therapists.yml
    with:
      environment: staging
    secrets: inherit
