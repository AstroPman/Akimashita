name: scraper-therapists (production)

# 本番は週次スケジュール + 手動実行。
on:
  schedule:
    # 毎週日曜 UTC 19:00 (= 月曜 JST 04:00)
    - cron: '0 19 * * 0'
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/scraper-therapists.yml
    with:
      environment: production
    secrets: inherit
